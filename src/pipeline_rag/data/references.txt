BM25 is a ranking function used to estimate the relevance of documents based on keywords.
GPT-2 generates text using a transformer-based neural network trained on large datasets.
Information retrieval helps systems fetch relevant documents based on user queries.